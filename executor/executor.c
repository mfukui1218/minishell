#include "minishell.h"

int	is_exec(char *token)
{
	if (str_has_str_first(token, ""))
		return (1);
	else if (str_has_str_first(token, ""))
		return (1);
	else if (str_has_str_first(token, ""))
		return (1);
	else if (str_has_str_first(token, ""))
		return (1);
	else if (str_has_str_first(token, ""))
		return (1);
	else if (str_has_str_first(token, ""))
		return (1);
	else if (str_has_str_first(token, ""))
		return (1);
	return (0);
}
